Using the Python - Pygbag tool so Pygame can run in the web browser.

All code is in main.py. The html file was created from the Pygbag tool, and I changed the html file, a little, for the info screen box to appear in the background.
